# Copy to .env and fill in real values. Never commit the real .env file.
# Not yet consumed by code in Stage 1 (pure game engine) — added here as part
# of the mandatory project scaffold; wired up starting Stage 5 (LLM) and
# Stage 8 (Gmail reporting).

OPENAI_API_KEY=
GOOGLE_CLIENT_SECRET_PATH=
REPORT_RECIPIENT_EMAIL=
